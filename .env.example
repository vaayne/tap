# Remote CDP WebSocket URL (optional, uses local Chrome if unset)
# TAP_WS_URL=wss://your-remote-browser/ws

# Chrome profile directory (optional, defaults to ~/.cache/tap/chrome-profile-$USER)
# TAP_PROFILE_DIR=

# Sites directory (optional, defaults to ./sites)
# TAP_SITES_DIR=./sites
